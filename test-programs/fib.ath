def fibrec(x)
  if (x < 3) then
    1
  else
    fibrec(x-1)+fibrec(x-2);

def fibiter(x)
  var a = 1, b = 1, c in
  (for i = 3, i < x in
    c = a + b :
	a = b :
	b = c) :
  b;

# call both
fibrec(10);
fibiter(10);
